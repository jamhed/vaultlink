{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cd src && GOOS=linux go build -o ../skaffold/vaultlink",
            "problemMatcher": "$go",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}